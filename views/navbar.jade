nav.navbar.navbar-fixed-top.navbar-default( role="navigation" )
    .navbar-header
        button.navbar-toggle.collapsed( type="button", data-toggle="collapse", data-target="#navbar-inverse", aria-expanded="false", aria-controls="navbar")
            span.sr-only Toggle navigation
            span.icon-bar
            span.icon-bar
            span.icon-bar
        a.navbar-brand(href="/") FauxtressBot
        div.navbar-brand(id="flipcountdownbox1")
    #navbar-inverse.collapse.navbar-collapse
        ul.nav.navbar-nav
            //- li.active: a( href="/") Home
            if (!user)
              li: a( href="#" onclick="") Link 2
            li: a( href="#contact" ) Link 1

            li.dropdown
                a.dropdown-toggle( href="#", data-toggle="dropdown", role="button", aria-expanded="false" ) Menu 
                    span.caret
                ul.dropdown-menu( role="menu" )
                    if (user)
                      li: a( href="/punch" ) !add
                      li.divider
                    li.dropdown-header Account
                    if (user)
                      li: a( href="/profile") Profile
                      li: a( href="/logout" ) Log Out
                    if (!user)
                      li: a( href="/login" ) Log In
                      li: a(href='/forgot') Forgot Password?
                      li.divider
                      li: a( href="/register" ) Sign Up